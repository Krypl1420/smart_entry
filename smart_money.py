import ib_api
from discord_api import 
IB = ib_api.initialize_ib()
def trade_range(range_high, range_low):
    while(True):
        ib_api.get_last_n_bars(IB,)
        candles = ib_api.
        if current_price => range_high:

        elif current